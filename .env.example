# NAC Code environment.
# None required yet — provider auth is delegated to the installed harness CLIs.
# Sensitive values (e.g. carrier-harness routing tokens) will use a write-only secret store, not this file.
